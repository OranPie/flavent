# `statistics`

## Overview
Basic statistics.

Import:
```flavent
use statistics
```

## API
- `mean(xs: List[Float]) -> Float`
- `median(xs: List[Float]) -> Float`
- `stdev(xs: List[Float]) -> Float`
