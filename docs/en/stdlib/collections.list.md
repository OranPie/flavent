# `collections.list`

Import:
```flavent
use collections.list
```

Selected API:
- `List[T] = Nil | Cons(T, List[T])`
- `nil/cons`
- `length`, `reverse`, `append`
- `get`, `last`, `take`, `drop`
- `contains`
- `rangeInt`
