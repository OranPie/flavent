# `collections.list`

导入：
```flavent
use collections.list
```

API（节选）：
- `List[T] = Nil | Cons(T, List[T])`
- `nil/cons`
- `length`, `reverse`, `append`
- `get`, `last`, `take`, `drop`
- `contains`
- `rangeInt`
