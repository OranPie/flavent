# `collections.*`

## 概述
持久化数据结构。

导入示例：
```flavent
use collections.list
use collections.map
use collections.set
use collections.queue
use collections.heap
```

子模块页面（后续可继续细拆）：
- `collections.list`
- `collections.map`
- `collections.set`
- `collections.queue`
- `collections.heap`
